apiVersion: apps/v1
kind: Deployment

metadata:
  name: backend

spec:
  replicas: 2

  selector:
    matchLabels:
      app: backend

  template:
    metadata:
      labels:
        app: backend

    spec:
      containers:
        - name: backend
          image: 058264118655.dkr.ecr.us-east-1.amazonaws.com/tech-challenge-1-backend:latest

          ports:
            - containerPort: 8080
